# MeetMind Runtime Assets Builder

Public helper repository used only to build bundled Python and wheelhouse assets
for MeetMind's app-managed Chatterbox runtime on native GitHub-hosted runners.

It does not contain the MeetMind desktop application source code.
